import React from "react";

const Wrapper = () => {
    const wrapperStyle = {
        border: '2px solid black',
        padding: '16px'
    };

    return (
        // style이 wrapperStyle인 div 태그를 반환
        <div style = {wrapperStyle}>

        </div>
    )
}

export default Wrapper;