// 함수형 컴포넌트
import React from 'react';

// props(컴포넌트의 속성)를 매개변수로 받아 JSX를 반환하는 함수형 컴포넌트
const Func1 = (props) => {
    return ( 
        <div>
            <header>
                <h1>함수형 컴포넌트 1</h1>
            </header>
        </div>
    );
}

export default Func1;
